name: SYSARCH-ACS Daily (Test GCC15 UEFI Only)

on:
  workflow_dispatch:

jobs:
  run:
    uses: ./.github/workflows/sysarch_ci.yml
    with:
      edk2-ref: edk2-stable202505
      toolchain: arm-dev,gcc
      run-uefi: true
      run-baremetal: false
      run-linux: false
    secrets: inherit
